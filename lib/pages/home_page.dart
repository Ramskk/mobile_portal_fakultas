import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('FST UMSIDA'),
      ),
      body: Center(
        child: Text('Selamat datang di portal FST UMSIDA'),
      ),
    );
  }
}
